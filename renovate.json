{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "description": "Formula/bsdoc.rb is generated by the release workflow, not a dependency manifest. Renovate's homebrew manager rewrote the darwin_arm64 url to a source archive (which has no bsdoc binary to install) and left `version` and the other three platforms stale, and every formula PR it merged triggered another release that re-staled the formula.",
      "matchFileNames": ["Formula/**"],
      "enabled": false
    }
  ]
}
